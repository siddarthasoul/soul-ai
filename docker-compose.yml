services:
  backend:
    build:
      context: ./backend
      dockerfile: docker/Dockerfile

    container_name: soul-backend

    expose:
      - "8000"

    env_file:
      - ./backend/.env

    extra_hosts:
      - "host.docker.internal:host-gateway"

    networks:
      - soul-network

    restart: unless-stopped

  nginx:
    image: nginx:alpine

    container_name: soul-nginx

    ports:
      - "80:80"

    volumes:
      - ./backend/docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro

    depends_on:
      - backend

    networks:
      - soul-network

    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: docker/Dockerfile

    container_name: soul-frontend

    ports:
      - "3000:3000"

    depends_on:
      - nginx

    networks:
      - soul-network

    restart: unless-stopped

networks:
  soul-network:
    driver: bridge